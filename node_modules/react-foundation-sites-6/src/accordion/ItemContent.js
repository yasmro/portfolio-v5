import React, {Component} from 'react';

export default class ItemComponent extends Component {

  render(){

    const contentStyle = this.props.active ? {display: 'block'} : {};

    return (
      <div className='accordion-content' role='tabpanel' style={contentStyle}>
        {this.props.children}
      </div>
    );
  }
}
